---
phase: 01-core-flow-stabilization
plan: 01-03
title: Final smoke verification
type: verification
wave: 3
depends_on: [01-02]
files_modified: [.planning/phases/01-core-flow-stabilization/01-03-SUMMARY.md]
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05]
requirements_addressed: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05]
user_setup: []
must_haves:
  truths:
    - "D-01: Final smoke must prove Contact.jsx uses contactApi.submit, not contactApi.send."
    - "D-03: Final smoke must prove failed contact submission does not clear form data."
    - "D-05: Final smoke must prove ProtectedRoute remains the primary checkout auth boundary."
    - "D-09: Final smoke must prove coupon input clears on successful application only."
    - "D-11: Final smoke may accept removeCoupon clearing local coupon state after attempting the API call."
    - "D-13: Final evidence must avoid timestamp-only checker churn."
    - "D-14: Final verification requires the spike checker and the frontend CRA build when dependencies are available."
    - "D-15: Payment and inventory WARN findings are non-blocking for Phase 1."
    - "D-16: react19-test-patterns, express-rest-api, and zustand-state-management are supporting guidance only, not extra gates."
  artifacts:
    - path: .planning/phases/01-core-flow-stabilization/01-03-SUMMARY.md
      provides: Final Phase 1 smoke evidence and any blocker notes
      contains: Verification
  key_links:
    - from: Frontend/Ecommerce-main/my-app/src/App.js
      to: Frontend/Ecommerce-main/my-app/src/components/ProtectedRoute.jsx
      via: checkout route guard
      pattern: ProtectedRoute
    - from: Frontend/Ecommerce-main/my-app/src/pages/Checkout.jsx
      to: Frontend/Ecommerce-main/my-app/src/store/cartStore.js
      via: applyCoupon return contract
      pattern: applyCoupon
---

# Phase 01 Plan 03: Final Smoke Verification

<objective>
Run final source, checker, and frontend build smoke verification for the stabilized core flow. This plan is the readiness gate before Phase 1 can be considered complete; it should create a concise summary of commands run, results, and any explicit blocker.
</objective>

## Context

Read these before verification:

- @.planning/phases/01-core-flow-stabilization/01-SPEC.md
- @.planning/phases/01-core-flow-stabilization/01-CONTEXT.md
- @.planning/phases/01-core-flow-stabilization/01-01-PLAN.md
- @.planning/phases/01-core-flow-stabilization/01-02-PLAN.md
- @Frontend/Ecommerce-main/my-app/package.json
- @.planning/spikes/001-core-flow-contract-check/check-contracts.mjs

## Recommendation

Use command evidence over narrative. The final summary should state the exact checker and build outcomes, and if the frontend build cannot run because dependencies are unavailable, record that as an explicit environment blocker rather than treating it as a pass.

## Artifacts This Phase Produces

- A final `01-03-SUMMARY.md` with verification evidence.

<tasks>
<task type="verify">
  <name>Run source smoke checks</name>
  <files>Frontend/Ecommerce-main/my-app/src/pages/Contact.jsx; Frontend/Ecommerce-main/my-app/src/pages/Checkout.jsx; Frontend/Ecommerce-main/my-app/src/store/cartStore.js; Backend/controllers/cartController.js; Frontend/Ecommerce-main/my-app/src/App.js</files>
  <action>
  Run focused `rg` checks for the Phase 1 source invariants: no `contactApi.send`, no demo catch-path success, no mock guest checkout branch, checkout route still wrapped by `ProtectedRoute`, coupon success path returns discount, and remove coupon has a missing-cart safe response.
  </action>
  <verify>
  Commands should show the expected positive patterns and no banned patterns. Any unexpected match must be resolved or documented as a blocker.
  </verify>
  <done>Source smoke checks support CORE-01 through CORE-05.</done>
</task>

<task type="verify">
  <name>Run contract checker gate</name>
  <files>.planning/spikes/001-core-flow-contract-check/check-contracts.mjs; .planning/spikes/001-core-flow-contract-check/results.json; .planning/spikes/001-core-flow-contract-check/contract-report.md</files>
  <action>
  Run the spike contract checker from the repository root. Inspect stdout and generated JSON. Do not keep new checker output if the only diff is `generated_at`.
  </action>
  <verify>
  `FAIL` count is zero or absent. Remaining findings, if any, are expected Phase 5 payment and Phase 4 inventory warnings.
  </verify>
  <done>The spike checker gate passes for Phase 1.</done>
</task>

<task type="verify">
  <name>Run frontend build when dependencies are available</name>
  <files>Frontend/Ecommerce-main/my-app/package.json; Frontend/Ecommerce-main/my-app/package-lock.json</files>
  <action>
  From `Frontend/Ecommerce-main/my-app`, run the CRA build command when dependencies are already installed. If `node_modules` is missing and `package-lock.json` exists, the executor may run dependency installation first if it can do so without editing package manifests. If install/build cannot run in the environment, record the exact blocker in the summary.
  </action>
  <verify>
  Build exits 0, or the summary contains the explicit dependency/environment blocker and the exact command that failed.
  </verify>
  <done>Frontend build confidence is recorded as pass or explicit blocker; it is not silently skipped.</done>
</task>

<task type="docs">
  <name>Create final plan summary</name>
  <files>.planning/phases/01-core-flow-stabilization/01-03-SUMMARY.md</files>
  <action>
  Write a concise summary with files changed by previous plans, command results, checker counts, build result, residual warnings, and confirmation that Phase 1 did not expand into deferred payment, inventory, broad tests, dependency upgrades, or architecture cleanup.
  </action>
  <verify>
  The summary includes a `Verification` section with the exact commands and pass/fail/blocker outcomes.
  </verify>
  <done>Final smoke evidence is available for phase completion review.</done>
</task>
</tasks>

<verification>
Minimum verification for this plan:

1. Focused `rg` source checks for banned and required patterns.
2. The spike contract checker from `.planning/spikes/001-core-flow-contract-check/check-contracts.mjs`.
3. The CRA build command from `Frontend/Ecommerce-main/my-app` when dependencies are available, or an explicit blocker summary if not.
</verification>

<threat_model>
The main risk is marking Phase 1 complete with only static source edits. This plan requires a repeatable checker gate and frontend build evidence where the environment permits it, while keeping known payment and inventory warnings deferred.
</threat_model>

<success_criteria>
- [ ] Focused source checks confirm the locked Phase 1 behaviors.
- [ ] The contract checker reports zero `FAIL` findings.
- [ ] Frontend build passes when dependencies are available, or the exact blocker is documented.
- [ ] Only payment and inventory warnings remain, and they are documented as later-phase work.
- [ ] A final summary records verification evidence for Phase 1.
</success_criteria>

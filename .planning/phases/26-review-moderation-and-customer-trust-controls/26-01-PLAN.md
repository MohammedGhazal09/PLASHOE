# Plan 26-01: Admin Review Moderation APIs

## Goal

Add protected admin review list/detail/moderation APIs with aggregate-safe status changes.

## Tasks

- Extend review validators for admin list, params, and moderation payloads.
- Add admin list and detail handlers.
- Add moderation handler for approve/hide.
- Recalculate product aggregates after moderation changes.
- Mount routes under `/api/admin/reviews`.

## Done

Implemented in `Backend/validators/review.js`, `Backend/controllers/reviewController.js`, `Backend/routes/adminReviewRoutes.js`, and `Backend/app.js`.

# Plan 25-01: Newsletter Model and APIs

## Goal

Add newsletter persistence plus public subscribe/unsubscribe APIs and protected admin visibility.

## Tasks

- Add `NewsletterSubscription` model.
- Add newsletter validators.
- Add public subscribe and unsubscribe controller handlers.
- Add protected admin list and summary handlers.
- Mount `/api/newsletter` routes with strict JSON parsing and a dedicated limiter.

## Done

Implemented in `Backend/models/NewsletterSubscription.js`, `Backend/validators/newsletter.js`, `Backend/controllers/newsletterController.js`, `Backend/routes/newsletterRoutes.js`, `Backend/app.js`, `Backend/config/security.js`, and `Backend/middleware/security.js`.

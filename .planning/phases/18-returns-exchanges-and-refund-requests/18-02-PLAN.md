# Plan 18-02 - Customer and Admin RMA UI

## Tasks

1. Add `returnsApi.js` customer wrapper and admin return methods in `adminApi.js`.
2. Extend `OrderDetail.jsx` with RMA request listing and eligible request form.
3. Add `AdminReturns.jsx` queue/detail/action UI.
4. Add Returns tab to `AdminConsole.jsx`.
5. Add frontend tests for customer request submission, ineligible state, admin queue actions, and API wrapper endpoints.

## Verification

```powershell
cd Frontend/Ecommerce-main/my-app
npm test -- returnsApi.test.js adminApi.test.js OrderDetail.test.jsx AdminReturns.test.jsx
```

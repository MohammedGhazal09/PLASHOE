# Plan 18-01 - Return/Exchange Model, Eligibility, and APIs

## Tasks

1. Add `ReturnRequest` model with request number, type, items, eligibility snapshot, refund intent, status, and status history.
2. Add validators for customer create/list/detail and admin list/status update routes.
3. Add RMA service for eligibility and transition rules.
4. Add customer `/api/returns` routes.
5. Add admin `/api/admin/returns` routes.
6. Add backend tests for auth, ownership, eligibility, duplicate quantity bounds, admin transitions, and refund-state isolation.

## Verification

```powershell
cd Backend
npm test -- return-request.test.js payment-webhook.test.js
```

---
phase: 02-automated-test-foundation
plan: 02-02
title: Backend core API route coverage
type: implementation
wave: 2
depends_on: [02-01]
files_modified: [Backend/test/auth.test.js, Backend/test/cart.test.js, Backend/test/order.test.js, Backend/test/contact.test.js, Backend/test/helpers/factories.js, Backend/test/helpers/auth.js]
autonomous: true
requirements: [TEST-02]
requirements_addressed: [TEST-02]
user_setup: []
must_haves:
  truths:
    - "D-09: Backend API tests are organized by route/resource: auth, cart/coupon, order, and contact."
    - "D-10: Auth tests cover register, login, current-user lookup, missing-token rejection, invalid-token rejection, and protected route access."
    - "D-11: Cart, coupon, and order tests cover happy paths plus key errors required by 02-SPEC.md, not exhaustive branch coverage."
    - "D-12: Include a path where a valid coupon applied to a cart affects order discount/totals and order creation clears the cart."
    - "D-13: Contact API tests cover successful public submission and required-field rejection."
    - "D-14: Backend assertions target status, success, message, data, totals, and key fields; avoid broad snapshots and status-only checks."
    - "D-29: Backend route tests run after backend app extraction and harness."
    - "D-30: Backend/.env.example remains untouched."
    - "D-31: No CI, payment, inventory transaction, admin fulfillment, dependency audit, or frontend tooling migration work enters Phase 2."
  artifacts:
    - path: Backend/test/auth.test.js
      provides: Auth route and protected route boundary coverage
      contains: /api/auth/register
    - path: Backend/test/cart.test.js
      provides: Cart and coupon API coverage
      contains: /api/cart/coupon
    - path: Backend/test/order.test.js
      provides: Order creation and empty-cart rejection coverage
      contains: /api/orders
    - path: Backend/test/contact.test.js
      provides: Public contact submission coverage
      contains: /api/contact
  key_links:
    - from: Backend/test/order.test.js
      to: Backend/controllers/orderController.js
      via: createOrder
      pattern: couponCode
    - from: Backend/test/cart.test.js
      to: Backend/controllers/cartController.js
      via: applyCoupon and removeCoupon
      pattern: data: null
---

# Phase 02 Plan 02: Backend Core API Route Coverage

<objective>
Add meaningful Supertest route coverage for auth, cart/coupon, order, and contact API paths using the disposable backend test harness from Plan 02-01.
</objective>

## Context

Read these before editing:

- @.planning/phases/02-automated-test-foundation/02-SPEC.md
- @.planning/phases/02-automated-test-foundation/02-CONTEXT.md
- @.planning/phases/02-automated-test-foundation/02-RESEARCH.md
- @.planning/phases/02-automated-test-foundation/02-PATTERNS.md
- @Backend/test/setup.js
- @Backend/test/helpers/factories.js
- @Backend/test/helpers/auth.js
- @Backend/app.js
- @Backend/routes/authRoutes.js
- @Backend/routes/cartRoutes.js
- @Backend/routes/orderRoutes.js
- @Backend/routes/contactRoutes.js
- @Backend/controllers/authController.js
- @Backend/controllers/cartController.js
- @Backend/controllers/orderController.js
- @Backend/controllers/contactController.js
- @Backend/models/User.js
- @Backend/models/Product.js
- @Backend/models/Cart.js
- @Backend/models/Coupon.js
- @Backend/models/Order.js
- @Backend/models/ContactMessage.js

## Recommendation

Exercise real routes with Supertest and real Mongoose documents. Keep assertions targeted to status and response contract fields so tests protect behavior without freezing entire Mongoose documents.

## Artifacts This Phase Produces

- New `Backend/test/auth.test.js`.
- New `Backend/test/cart.test.js`.
- New `Backend/test/order.test.js`.
- New `Backend/test/contact.test.js`.
- Helper additions only where needed for fixtures or auth headers.

<tasks>
<task type="code">
  <name>Add auth route and protected-boundary tests</name>
  <read_first>
  Backend/test/app.test.js
  Backend/test/helpers/factories.js
  Backend/test/helpers/auth.js
  Backend/routes/authRoutes.js
  Backend/controllers/authController.js
  Backend/middleware/auth.js
  Backend/models/User.js
  </read_first>
  <files>Backend/test/auth.test.js; Backend/test/helpers/factories.js; Backend/test/helpers/auth.js</files>
  <action>
  Add `Backend/test/auth.test.js` with Supertest tests for `POST /api/auth/register`, duplicate registration, `POST /api/auth/login`, invalid login, authenticated `GET /api/auth/me`, missing-token rejection, and invalid-token rejection. Assert exact expected statuses: `201`, `400`, `200`, `401`, `200`, `401`, and `401`. Assert response body includes `success`, relevant `message` values for failures, user fields, and token for successful auth.
  </action>
  <verify>
  `cd Backend && npm test -- auth.test.js` exits 0, or if Vitest filtering by file is unreliable, `cd Backend && npm test` exits 0 and includes auth tests.
  </verify>
  <acceptance_criteria>
  - Register/login/me success paths are covered.
  - Missing and invalid bearer tokens are rejected.
  - Assertions check response envelopes and token presence.
  </acceptance_criteria>
</task>

<task type="code">
  <name>Add cart and coupon route tests</name>
  <read_first>
  Backend/test/helpers/factories.js
  Backend/test/helpers/auth.js
  Backend/routes/cartRoutes.js
  Backend/controllers/cartController.js
  Backend/models/Product.js
  Backend/models/Cart.js
  Backend/models/Coupon.js
  </read_first>
  <files>Backend/test/cart.test.js; Backend/test/helpers/factories.js</files>
  <action>
  Add `Backend/test/cart.test.js` with Supertest tests for protected cart rejection without token, `GET /api/cart` creating or returning a cart, `POST /api/cart/items` adding a product with valid size, invalid product or invalid size rejection, `PUT /api/cart/items/:itemId` quantity update, `DELETE /api/cart/items/:itemId`, `DELETE /api/cart`, valid `POST /api/cart/coupon`, invalid coupon rejection, minimum-order rejection, and `DELETE /api/cart/coupon` returning `success: true` and `data: null` when the user has no cart.
  </action>
  <verify>
  `cd Backend && npm test -- cart.test.js` exits 0, or `cd Backend && npm test` exits 0 with cart tests included.
  </verify>
  <acceptance_criteria>
  - Cart operations update persisted cart state.
  - Valid coupon stores uppercase `couponCode` and numeric percentage `discount`.
  - Missing-cart coupon removal returns `data: null`.
  - Tests do not rely on production MongoDB or seed data.
  </acceptance_criteria>
</task>

<task type="code">
  <name>Add order route tests for cart checkout behavior</name>
  <read_first>
  Backend/test/helpers/factories.js
  Backend/test/helpers/auth.js
  Backend/routes/orderRoutes.js
  Backend/controllers/orderController.js
  Backend/models/Cart.js
  Backend/models/Coupon.js
  Backend/models/Order.js
  </read_first>
  <files>Backend/test/order.test.js; Backend/test/helpers/factories.js</files>
  <action>
  Add `Backend/test/order.test.js` with Supertest tests for missing-token rejection, empty-cart order rejection, missing required shipping field rejection, and successful `POST /api/orders` from a cart. The successful path must create a cart with at least one product, apply or pre-set a valid coupon on the cart, submit a shipping address with `firstName`, `lastName`, `country`, `street`, `city`, `state`, `zipCode`, and `phone`, and assert `201`, `success: true`, `subtotal`, `discount`, `total`, `couponCode`, order items, coupon `usedCount` increment, and cart cleared afterward.
  </action>
  <verify>
  `cd Backend && npm test -- order.test.js` exits 0, or `cd Backend && npm test` exits 0 with order tests included.
  </verify>
  <acceptance_criteria>
  - Empty carts cannot create orders.
  - Required shipping fields are enforced.
  - Coupon percentage affects order total.
  - Order creation clears cart items, couponCode, and discount.
  </acceptance_criteria>
</task>

<task type="code">
  <name>Add public contact route tests</name>
  <read_first>
  Backend/test/helpers/factories.js
  Backend/routes/contactRoutes.js
  Backend/controllers/contactController.js
  Backend/models/ContactMessage.js
  </read_first>
  <files>Backend/test/contact.test.js</files>
  <action>
  Add `Backend/test/contact.test.js` with Supertest tests for successful public `POST /api/contact` and required-field rejection. Success should send `name`, `email`, `subject`, and `message`; assert status `201`, `success: true`, `message: "Message sent successfully"`, and stored data fields. Rejection should omit a required field and assert status `400`, `success: false`, and `message: "Name, email and message are required"`.
  </action>
  <verify>
  `cd Backend && npm test -- contact.test.js` exits 0, or `cd Backend && npm test` exits 0 with contact tests included.
  </verify>
  <acceptance_criteria>
  - Public contact success is covered.
  - Required-field validation is covered.
  - No admin fulfillment behavior is added.
  </acceptance_criteria>
</task>

<task type="verify">
  <name>Run complete backend route suite twice</name>
  <read_first>
  Backend/package.json
  Backend/vitest.config.js
  Backend/test/setup.js
  </read_first>
  <files>Backend/test/auth.test.js; Backend/test/cart.test.js; Backend/test/order.test.js; Backend/test/contact.test.js</files>
  <action>
  Run `cd Backend && npm test` twice in a row to prove the disposable Mongo setup does not leak state between runs. If the second run fails due to state leakage, fix cleanup before completing this plan.
  </action>
  <verify>
  Both backend test runs exit 0.
  </verify>
  <acceptance_criteria>
  - `cd Backend && npm test` exits 0 twice consecutively.
  - No test requires manual seeded data.
  - No test connects to production MongoDB.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Minimum verification for this plan:

1. `cd Backend && npm test`
2. Repeat `cd Backend && npm test`
3. Inspect failures, if any, for leaked state or missing fixture setup before moving on.
</verification>

<threat_model>
Threats mitigated: auth bypass regressions, misleading response envelopes, cart/coupon/order state drift, public contact validation regression, and tests passing against mocked controllers while real routes are broken. This plan does not introduce rate limiting, new validation libraries, payment processing, inventory transactions, or admin fulfillment.
</threat_model>

<success_criteria>
- [ ] TEST-02: Auth success and rejection paths are covered.
- [ ] TEST-02: Cart add/update/remove/clear and coupon success/failure paths are covered.
- [ ] TEST-02: Missing-cart coupon removal returns `data: null` in a test.
- [ ] TEST-02: Order creation from cart and empty-cart rejection are covered.
- [ ] TEST-02: Contact success and required-field rejection are covered.
- [ ] Backend suite passes twice consecutively.
</success_criteria>

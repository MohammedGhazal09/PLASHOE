---
phase: 05-production-payments
plan: 05-05
title: Payment docs, static checker, and final verification
type: implementation
wave: 5
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified: [docs/API.md, docs/CONFIGURATION.md, docs/TESTING.md, .planning/spikes/001-core-flow-contract-check/check-contracts.mjs, .planning/spikes/001-core-flow-contract-check/results.json, .planning/spikes/001-core-flow-contract-check/contract-report.md]
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04]
requirements_addressed: [PAY-01, PAY-02, PAY-03, PAY-04]
user_setup: []
must_haves:
  truths:
    - "D-44: Update docs/API.md, docs/CONFIGURATION.md, and docs/TESTING.md for payment contracts, statuses, webhooks, env vars, local webhook notes, and test commands."
    - "D-45: Update the static contract checker from payment missing warning to a production payment signal."
    - "D-46: Never commit Stripe secrets, local .env contents, or secret-looking test values."
    - "D-47: Backend tests use Vitest/Supertest/MongoMemoryReplSet."
    - "D-49: Webhook signature tests use local signed payloads through the real Express route."
    - "D-50: Backend coverage includes checkout-start, retry, compensation, transitions, duplicates, invalid signatures, unresolved failures, failure/expiry restore, and refunds."
    - "D-51: Frontend coverage includes checkout redirect and return states."
    - "D-52: Browser E2E and live Stripe CLI are optional/manual, not required automated gates."
  artifacts:
    - path: docs/API.md
      provides: Payment API and webhook contract
      contains: /api/webhooks/stripe
    - path: docs/CONFIGURATION.md
      provides: Payment environment variable documentation
      contains: STRIPE_SECRET_KEY
    - path: docs/TESTING.md
      provides: Deterministic payment test documentation
      contains: payment-webhook.test.js
    - path: .planning/spikes/001-core-flow-contract-check/check-contracts.mjs
      provides: Static production payment readiness signal
      contains: payment-production-readiness
  key_links:
    - from: docs/API.md
      to: Backend/controllers/orderController.js
      via: checkout-start response shape
      pattern: payment.checkoutUrl
    - from: docs/API.md
      to: Backend/controllers/webhookController.js
      via: webhook status and retry semantics
      pattern: /api/webhooks/stripe
    - from: docs/TESTING.md
      to: Backend/test/payment-webhook.test.js
      via: deterministic webhook verification command
      pattern: Stripe-Signature
---

# Phase 05 Plan 05: Payment Docs, Static Checker, and Final Verification

<objective>
Document the production payment contract, update the retained static checker so it recognizes the new payment implementation, and run the full Phase 05 verification set.
</objective>

## Context

Read these before editing:

- @.planning/phases/05-production-payments/05-SPEC.md
- @.planning/phases/05-production-payments/05-CONTEXT.md
- @.planning/phases/05-production-payments/05-RESEARCH.md
- @.planning/phases/05-production-payments/05-01-PLAN.md
- @.planning/phases/05-production-payments/05-02-PLAN.md
- @.planning/phases/05-production-payments/05-03-PLAN.md
- @.planning/phases/05-production-payments/05-04-PLAN.md
- @docs/API.md
- @docs/CONFIGURATION.md
- @docs/TESTING.md
- @.planning/spikes/001-core-flow-contract-check/check-contracts.mjs

## Recommendation

Treat docs and checker updates as part of the payment contract, not cleanup. They are the bridge between backend/frontend behavior and future deployment/admin phases.

## Artifacts This Plan Produces

- API docs for checkout-start payment response, payment statuses, webhook endpoint, duplicate/retry behavior, refund states, and error semantics.
- Configuration docs for `PAYMENTS_ENABLED`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `PAYMENT_SUCCESS_URL`, and `PAYMENT_CANCEL_URL`.
- Testing docs for mocked Stripe provider tests, signed webhook fixture tests, frontend redirect/return tests, optional Stripe CLI notes, and Phase 05 commands.
- Static checker update that marks production payment readiness as passing when demo copy is removed and backend payment/webhook artifacts exist.
- Final verification evidence for backend, frontend, build, and static checker.

<tasks>
<task type="docs">
  <name>Document payment API and configuration contracts</name>
  <read_first>
  docs/API.md
  docs/CONFIGURATION.md
  Backend/controllers/orderController.js
  Backend/controllers/webhookController.js
  Backend/config/env.js
  Backend/models/Order.js
  .planning/phases/05-production-payments/05-SPEC.md
  </read_first>
  <files>docs/API.md; docs/CONFIGURATION.md</files>
  <action>
  Update `docs/API.md` so `POST /api/orders` is documented as checkout-start returning `data.order` plus `data.payment.provider`, `data.payment.checkoutUrl`, and provider session data. Document payment statuses `requires_payment`, `payment_pending`, `paid`, `payment_failed`, `payment_canceled`, `refunded`, `partially_refunded`, and `not_required`; document `POST /api/webhooks/stripe` as unauthenticated but Stripe-signature verified; document invalid signature `400`, duplicate event no-op success, unresolved webhook retry `500`, and provider-origin refund behavior. Update `docs/CONFIGURATION.md` with payment env vars and fake example placeholders only.
  </action>
  <verify>
  `rg "paymentStatus|/api/webhooks/stripe|PAYMENTS_ENABLED|STRIPE_SECRET_KEY|PAYMENT_SUCCESS_URL|payment.checkoutUrl" docs/API.md docs/CONFIGURATION.md`
  </verify>
  <acceptance_criteria>
  - API docs show checkout-start payment response shape.
  - API docs list every Phase 05 payment status.
  - API docs explain webhook signature, duplicate, retry, failure, expiry, and refund behavior.
  - Configuration docs list every required payment variable and `PAYMENTS_ENABLED`.
  - Docs contain no `sk_live`, `sk_test`, `whsec_`, or real secret-looking values beyond safe placeholder words.
  </acceptance_criteria>
</task>

<task type="docs">
  <name>Document deterministic payment tests and optional local webhook tooling</name>
  <read_first>
  docs/TESTING.md
  Backend/test/order.test.js
  Backend/test/payment-webhook.test.js
  Backend/test/payment-state.test.js
  Frontend/Ecommerce-main/my-app/src/pages/Checkout.test.jsx
  Frontend/Ecommerce-main/my-app/src/pages/CheckoutReturn.test.jsx
  .planning/phases/05-production-payments/05-RESEARCH.md
  </read_first>
  <files>docs/TESTING.md</files>
  <action>
  Update `docs/TESTING.md` with Phase 05 backend commands for `order.test.js`, `payment-state.test.js`, and `payment-webhook.test.js`; frontend commands for `Checkout.test.jsx`, `CheckoutReturn.test.jsx`, and `ordersApi.test.js`; and final full-suite/build/static-check commands. Document that tests use mocked provider seams and locally signed webhook payloads, not live Stripe credentials or network calls. Add optional manual Stripe CLI notes for local developer exploration only, clearly not an automated gate.
  </action>
  <verify>
  `rg "payment-webhook.test.js|CheckoutReturn.test.jsx|signed webhook|Stripe CLI|not an automated gate" docs/TESTING.md`
  </verify>
  <acceptance_criteria>
  - Testing docs list backend payment-focused command(s).
  - Testing docs list frontend payment-focused command(s).
  - Testing docs state live Stripe calls are not required for automated tests.
  - Optional Stripe CLI notes are labeled manual/optional.
  - Phase 04 testing context remains available and is not deleted.
  </acceptance_criteria>
</task>

<task type="verify">
  <name>Update static checker and run final Phase 05 gates</name>
  <read_first>
  .planning/spikes/001-core-flow-contract-check/check-contracts.mjs
  docs/TESTING.md
  Backend/package.json
  Frontend/Ecommerce-main/my-app/package.json
  </read_first>
  <files>.planning/spikes/001-core-flow-contract-check/check-contracts.mjs; .planning/spikes/001-core-flow-contract-check/results.json; .planning/spikes/001-core-flow-contract-check/contract-report.md; docs/TESTING.md</files>
  <action>
  Update the `payment-production-readiness` static checker so it passes when demo payment copy is gone and the backend contains payment state, provider checkout, and webhook artifacts. Run backend focused payment tests, full backend tests, frontend focused payment tests, full frontend tests, frontend build, and the static checker. Commit generated checker output only when PASS/WARN/FAIL semantics change; do not commit timestamp-only churn. Record final command results in `docs/TESTING.md` if the docs already maintain a verification snapshot.
  </action>
  <verify>
  `cd Backend && npm test -- order.test.js payment-state.test.js payment-webhook.test.js`
  `cd Backend && npm test`
  `cd Frontend/Ecommerce-main/my-app && npm test -- Checkout.test.jsx CheckoutReturn.test.jsx ordersApi.test.js --watchAll=false`
  `cd Frontend/Ecommerce-main/my-app && npm test -- --watchAll=false`
  `cd Frontend/Ecommerce-main/my-app && npm run build`
  `node .planning/spikes/001-core-flow-contract-check/check-contracts.mjs`
  </verify>
  <acceptance_criteria>
  - Backend focused payment tests pass.
  - Full backend test suite passes.
  - Frontend focused payment tests pass.
  - Full frontend test suite passes.
  - Frontend production build passes with no new undocumented warnings.
  - Static checker has no `FAIL` findings and no stale payment readiness warning.
  </acceptance_criteria>
</task>
</tasks>

<verification>
1. `cd Backend && npm test -- order.test.js payment-state.test.js payment-webhook.test.js`
2. `cd Backend && npm test`
3. `cd Frontend/Ecommerce-main/my-app && npm test -- Checkout.test.jsx CheckoutReturn.test.jsx ordersApi.test.js --watchAll=false`
4. `cd Frontend/Ecommerce-main/my-app && npm test -- --watchAll=false`
5. `cd Frontend/Ecommerce-main/my-app && npm run build`
6. `node .planning/spikes/001-core-flow-contract-check/check-contracts.mjs`
7. `rg "sk_live|sk_test|whsec_" . --glob "!Backend/node_modules/**" --glob "!Frontend/Ecommerce-main/my-app/node_modules/**" --glob "!package-lock.json"`
</verification>

<success_criteria>
- [ ] PAY-01: Docs and checker reflect demo-payment removal and production checkout start.
- [ ] PAY-02: Docs list independent payment status fields and labels.
- [ ] PAY-03: Docs cover success, failure, cancellation/expiry, and refund states.
- [ ] PAY-04: Docs and test snapshot cover webhook tests.
- [ ] Full backend, frontend, build, and static checker verification is recorded.
</success_criteria>

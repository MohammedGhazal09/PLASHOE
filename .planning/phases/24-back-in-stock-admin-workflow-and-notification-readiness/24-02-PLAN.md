# Plan 24-02: Admin Retention-Demand UI

## Goal

Expose restock demand in the admin console with actionable filters and status controls.

## Tasks

- Add frontend admin API wrappers.
- Add `AdminBackInStock` section with summary, largest demand, filters, table, loading, error, empty, and notice states.
- Register the section in `AdminConsole` as `Restock`.
- Keep controls native and accessible.

## Done

Implemented in `Frontend/Ecommerce-main/my-app/src/api/adminApi.js`, `Frontend/Ecommerce-main/my-app/src/pages/admin/AdminBackInStock.jsx`, and `Frontend/Ecommerce-main/my-app/src/pages/AdminConsole.jsx`.
